

## have functions for handling credentials, and pass the data object as a paremter into all of them
## or alternatively, make a class and pass the state into the clas. that should actualyl kind of do it to be honest

##hae like an agent class, which all the shit like uh, current connection, proofs n shit in memory
##or maybe not honestly. probably more robust to retrieve everything. .


def parse_cred(inbound=True):
    if inbound:
        return None
    else:
        return True

    return None



def parse_inbound_cred():
    return None


def parse_inbound_cred():
    return None
